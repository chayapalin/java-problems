// Define a class named Account. It must have three fields: a long field balance, string field ownerName, and boolean flag locked.

class Account {
    long balance;
    String ownerName;
    boolean locked;
}
